meta:
  environment: python
  toolchain:
    name: pip
    version: 3.11
build:
  requirementsPath: requirements.txt
run:
  scriptName: python3 -m app.main
  persistenceMount: /data
  containerPort: 80
